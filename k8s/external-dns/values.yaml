provider:
  name: aws

policy: sync

registry: txt
txtOwnerId: eks-cluster

domainFilters:
  - ahmedo.co.uk

serviceAccount:
  create: true
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::409987738946:role/eks-cluster-external-dns-role